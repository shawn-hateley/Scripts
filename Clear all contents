function clearAllContents() { //This clears the contents of all the sheets in a spreadsheet
  var sheets = SpreadsheetApp.getActiveSpreadsheet().getSheets();
  
  for(var s = 0; s < sheets.length; s++){  
    var sheet = sheets[s];
      sheet.clearContents();
   };
};